#系统级别的配置
spring:
  cloud:
    config:
      name: config-client #从github上读取资源名称，不需要后缀
      profile: test
      label: master
      uri: http://localhost:3344
      #以上其实就是:http://localhost:3344/config-client-dev.yml